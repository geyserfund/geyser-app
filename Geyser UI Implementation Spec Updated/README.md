
  # Geyser UI Implementation Spec

  This is a code bundle for Geyser UI Implementation Spec. The original project is available at https://www.figma.com/design/ePN3OPo3p4ZSEzPBsaNesc/Geyser-UI-Implementation-Spec.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  