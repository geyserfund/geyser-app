export const hasShipping = (name: string) => (
	name !== 'dayofgenesis'
  && name !== 'lightningrebel'
  && name !== 'bitcoinballers'
  && name !== 'anatomyofbitcoin'
  && name !== 'bitcoinforfairness'
	&& name !== 'orangepillapp'
  && name !== 'anonymal'
  && name !== 'rapaygo'
  && name !== 'bitcoinracing'
  && name !== 'bitcointwitter'
  && name !== 'binkbonkbank'
);
